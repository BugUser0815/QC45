../jre2/bin/java -cp "DBUpdate.jar:lib/derby.jar" dbupdate.DBUpdate
# Example only. Choose the replacement locally and never commit a real password:
# UPDATE MOBIE.CPUSER SET PASS = '<REPLACE_LOCALLY>' WHERE UNAME = 'efacec'
